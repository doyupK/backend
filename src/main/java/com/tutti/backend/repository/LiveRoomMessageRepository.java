package com.tutti.backend.repository;

import com.tutti.backend.domain.LiveRoomMessage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LiveRoomMessageRepository extends JpaRepository<LiveRoomMessage, Long>{


}
